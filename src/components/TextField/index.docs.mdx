import { Canvas, Meta } from "@storybook/blocks";

import * as TextField from "./index.stories";

<Meta of={TextField} />

# TextField

Reusable text field component responsible for accepting user input. Style customization is possible through `sx` prop.

<Canvas of={TextField.Default} />
