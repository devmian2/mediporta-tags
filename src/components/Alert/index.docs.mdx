import { Canvas, Meta } from "@storybook/blocks";

import * as AlertStories from "./index.stories";

<Meta of={AlertStories} />

# Alert

Reusable alert component responsible for displaying important messages.
Style customization is possible through `sx` prop. It accepts `severity` prop to display an alert in following variants: `"info" | "error" | "success" | "warning"`.
Content can be passed through `children` prop.

<Canvas of={AlertStories.Default} />
